import BaseConfig from "@razzia/web/features/quizz/components/QuestionEditor/QuestionEditorConfig/BaseConfig"

// The settings of a multiple choice: points, speed bonus, penalty and the
// scoring mode, which reads the passages as answers.
const HighlightConfig = () => <BaseConfig />

export default HighlightConfig
